@using Microsoft.AspNetCore.Identity
@model IEnumerable<IdentityUser>

@{
    ViewBag.Title = "Index";
}

<br>
<center>
<h2>Mantenimiento de usuarios </h2>
</center>


<p>
      <button class="btn btn-success ml-2" onclick="location.href='@Url.Action("Create","UsersAdmin")';return false;">Añadir</button>
  @*  @Html.ActionLink("Nuevo", "Create") *@
</p>

@*<div class="">
    @using (Html.BeginForm()) 
    {
            <p>
                Email: @Html.TextBox("search", null, new { @class = "m-2" }) 
                <input type="submit" value="Buscar" class="btn btn-outline-success btn-sm" />
            </p>   
    }
</div>*@
<table class="table" id="tablaUsuarios">
    <thead class="thead-dark">
    <tr class="">
        <th class="">
            Nombre de usuario:
        </th>
        <th>

        </th>
    </tr>
    </thead>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>

            <td>
               @* @Html.ActionLink("Editar", "Edit", new { id = item.Id }) | *@
               <button class="btn btn-primary ml-2" onclick="location.href='@Url.Action("Details","UsersAdmin",new{ id = item.Id})';return false;">Detalles</button>
               <button class="btn btn-danger ml-2" onclick="location.href='@Url.Action("Delete","UsersAdmin",new{ id = item.Id})';return false;">Eliminar</button>
             @*   @Html.ActionLink("Detalles", "Details", new { id = item.Id }) |
                @Html.ActionLink("Borrar", "Delete", new { id = item.Id }) *@
            </td>
        </tr>
    }

</table>
